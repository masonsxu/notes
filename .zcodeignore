# --- OS / Editor ---
.DS_Store
.DS_Store?
Icon?
._*
.AppleDouble
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini
*.swp
*.swo
*~

# --- 工具 / IDE 本地状态 ---
.omo/
.playwright-mcp/
.zcode/

# --- Node / pnpm ---
node_modules/
pnpm-debug.log*

# --- VitePress / apps/site ---
# 产物与 sync.mjs 物化内容均可由顶层 notes/ + reports/ 重建
apps/site/dist/
apps/site/docs/.vitepress/cache/
apps/site/docs/.vitepress/gen/
apps/site/docs/gen/
apps/site/docs/public/files/

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# --- 虚拟环境 ---
.venv/
venv/
env/

# --- uv ---
# uv.lock 需要提交(锁定依赖版本);只忽略临时缓存
.uv/

# --- ML / 数据科学 ---
# 学习脚本生成的图表、中间产物,只忽略约定的输出目录
code/**/output/
code/**/outputs/
code/**/runs/
code/**/mlruns/
learning/**/output/
learning/**/outputs/
learning/**/runs/
learning/**/mlruns/

# 大数据集 / 模型权重,本地存放不进库(按需在具体项目下加更精确的规则)
*.h5
*.parquet
*.feather
*.ckpt
*.pt
*.onnx

# Jupyter
.ipynb_checkpoints/

# Optuna / SHAP / 训练日志
optuna.db
optuna-journal*
*.sqlite
*.sqlite3

# ===== ↑ 以上同步自 .gitignore（「从 .gitignore 同步」只重写以上部分）=====
.git/
.hg/
.svn/
node_modules/
bower_components/
jspm_packages/
site-packages/
coverage/
htmlcov/
lcov-report/
cmakefiles/
cmake-build-*/
bazel-*/
pods/
deriveddata/
storybook-static/
playwright-report/
test-results/
allure-results/
allure-report/
cdk.out/
*.egg-info/
*.dist-info/
eggs/
pip-wheel-metadata/
wheels/
# ----- ↑ 以上为 ZCode 默认排除规则（自定义规则请写在本行下方，不会被同步/恢复改动）-----
# 自定义规则写在下方（本行提示可删除）
