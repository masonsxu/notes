这份大模型落地宣讲材料专门结合了**半导体先进封装 TD Testing 部门**的实际业务场景（Pre-DC 测试、测试 Flow、机台自动化、Log/报错排查、良率溯源等），兼顾了**部门新人**的入门引导和**老板/组内**对成本控本与效能提升的诉求。

材料已分为两大主题，结构清晰、内容落地，可以直接作为编写 PPT 的 Outline 和逐字稿/演讲备注（Speaker Notes）。

---

# 主题一：《公司大模型工具使用入门》

> **目标受众**：部门新人、组内工程师
> **核心目的**：手把手教大家开通权限、选对界面、理解 CIMICode 核心概念，并快速将 AI 融入 TD Testing 的日常工作。

---

## 模块一：CIMICode 快速上手与权限申请

### 1. 为什么推荐大家申请 Coding 权限？

* **权限区分**：
* **非 Coding 权限**：仅限使用 Web 网页版。适合纯文档阅读、邮件草稿生成等简单场景。
* **Coding 权限**：支持安装本地 CIMICode（包含桌面版 Desktop 和终端版 TUI）。


* **推荐建议**：**强烈建议全员申请 Coding 权限**。
* **原因**：TD Testing 的日常工作（如 Log 解析、自动化脚本开发、SECS/GEM 或机台接口对接）依赖本地代码库和本地数据文件。桌面版和 TUI 能够直接挂载本地工作区，自动化处理本地测试文件，灵活性和使用场景远超 Web 网页版。



### 2. CIMICode 的三种形态与适用场景

| 形态 | 适用场景 | 优势/推荐使用习惯 |
| --- | --- | --- |
| **桌面版 (Desktop)** | 代码开发、文档撰写、数据分析、PPT 内容整理 | 视觉体验好，支持多窗口、图表预览和可视化项目管理。 |
| **终端版 (TUI)** | 服务器端自动化、Linux 跑脚本、排查机台报错 Log | 直接运行在 Linux/WSL 或机台服务器终端，无需图形界面，快捷高效。 |
| **Web 网页版** | 手机/移动端临时查阅、非敏感非 Coding 场景 | 快捷无需配置，适合突发性的文字处理或头脑风暴。 |

---

## 模块二：CIMICode 核心机制解析

在使用 CIMICode 之前，必须搞清楚系统里的几个核心概念，它们构成了大模型协助开发的骨架：

```
+-----------------------------------------------------------------------+
|                              CIMICode                                 |
|                                                                       |
|   [ AGENTS.md ] ---------> 上下文注入 (岗位背景/测试标准/规范)          |
|         |                                                             |
|   [ Model / Plan ] -----> 算力引擎 (DeepSeek v4 Flash / GLM / Kimi)   |
|         |                                                             |
|   [ Plan / Build ] -----> 执行模式 (Plan 规划算法 -> Build 自动落地)   |
|         |                                                             |
|   [ Skills / Tools ] ---> 扩展能力 (Web Search, Grill-Me, Auto-Approve)|
+-----------------------------------------------------------------------+

```

1. **Model（大模型）**：系统的“大脑”。负责推理、理解指令和生成代码/文本。
2. **Plan（代币与方案）**：模型的计费与能力套餐，需根据任务复杂度合理配置（详见主题二的成本控制）。
3. **Plan / Build 模式（规划与执行）**：
* **Plan 模式**：大模型只思考、拆解步骤、列出 Plan，**不修改任何文件**。
* **Build 模式**：大模型根据确认好的 Plan，**开始真正执行代码修改、文件写入和命令调用**。


4. **Skills（技能包）**：给大模型安装的“外挂工具”（如联网搜索 `web_search`、需求提炼 `grill_me`、自动执行等），大幅扩展大模型的能力边界。
5. **打开项目（Project Directory）**：将你的测试代码库、测试 Flow 配置或 Log 文件夹作为 Context（上下文）导入 CIMICode，大模型即可感知整个项目的全貌。
6. **`AGENTS.md`（系统人设与规则文件）**：存放在 `~/.cimi/cimicode/AGENTS.md`，是大模型的“工作指南”。定义了大模型的角色背景、半导体专业术语以及你的编码/文档偏好。

---

## 模块三：TD Testing 场景下的日常工作实战 Demo

大模型不仅仅是“聊天机器人”，在 TD Testing 的日常工作中，它是全能的“数字助理”：

```
                   ┌─── 1. 文件与邮件处理：提取测试异常/整理报告
                   ├─── 2. 编写 SOP 与 PPT：封装机台操作与测试 Flow
TD Testing 落地场景 ├─── 3. 脚本开发与自动化：Python/Shell/Log 解析
                   └─── 4. 实验设计 (DOE)：基于已有 Pre-DC 数据设计 Matrix

```

### 实战 Demo 1：文件整理与邮件读取（测试异常通报）

* **场景**：收到了机台供应商或产线发来的多封报错邮件及 Raw Log，需要快速总结。
* **指令示例**：
> “请读取 Downloads/logs 目录下的 5 个报错日志文件，提取出所有的 Error Code、发生频次以及对应的 Pre-DC 测试 Channel，整理成一份 Markdown 表格，并草拟一封回复给产线工程师的邮件。”



### 实战 Demo 2：自动化代码开发（测试机台 Log 解析脚本）

* **场景**：Pre-DC 阶段产生的大量测试文件格式复杂，需要写一个 Python 脚本清洗数据。
* **指令示例**：
> “写一个 Python 脚本，解析当前目录下所有 `.csv` 格式的 Pre-DC 测试数据，自动计算每批次 (Lot) 的 CPK 和 yield，并将 Yield < 98% 的 Wafer ID 标红导出为 Excel。”



### 实战 Demo 3：编写测试 SOP 与 PPT 大纲

* **场景**：新增了一台 Pre-DC 探针台（Prober），需要编写机台自动化测试 SOP。
* **指令示例**：
> “根据当前项目中的 `prober_driver.py` 驱动代码，生成一份《Pre-DC 机台自动化测试标准操作规程 (SOP)》，包含：环境准备、机台初始化、测试 Flow 挂载、常见报错（如 Contact Failure）排查步骤。”



### 实战 Demo 4：基于已有测试数据设计 DOE 实验

* **场景**：Pre-DC 测试中发现某一类 Bump 接触电阻（Rc）偏高。
* **指令示例**：
> “这是我们最近 3 批次 Pre-DC 的接触电阻数据 `rc_data.json`。请分析数据分布，结合先进封装 Micro-bump 的常见失效机制，为我设计一份 3 因子 2 水平的 DOE 实验方案，以优化针尖压力 (Overdrive) 和清洗周期。”



---

# 主题二：《大模型成本优化与高级功能配置》

> **目标受众**：老板、组内资深工程师、TL
> **核心目的**：解决“用不起”、“用不好”和“效率低”三大痛点，实现组内大模型使用降本增效。

---

## 模块一：Token 成本现状与降本策略（老板最关心的部分）

### 1. 公司 Token 成本对比与现状分析

| 模型 | 公司内部 Token 算力单价 | 相比公网倍率 | 推荐使用场景 |
| --- | --- | --- | --- |
| **DeepSeek v4 Flash** | **极低** | **基准** | **日常 80% 场景**：简单代码编写、Log 解析、格式转换、简单文档整理 |
| **DeepSeek v4 Pro** | 极高 | ~5x | **复杂逻辑推理**：复杂 Bug 深度排查、测试架构设计、算法推导 |
| **GLM 5.1 / 5.2** | 高 | ~5x | **长文本与中文公文**：长篇 SOP 编写、跨文件复杂整理 |
| **Kimi 2.7** | 高 | ~5x | **超长上下文解析**：超大型 Log 校验、海量文档对比 |

> ⚠️ **警告**：除 Flash 模型外，高阶模型成本比公网贵 5 倍左右！如果不加限制地全局使用 Pro/GLM 模型，单人月度账单将极高。

### 2. 降本增效“组合拳”法则

1. **80/20 法则（梯队模型策略）**：
* 默认开启 **DeepSeek v4 Flash** 进行日常对话、代码草稿和文本处理。
* 仅在 Flash 模型遇到瓶颈（如多次修改代码未解决、逻辑推理死循环）时，才切换至 Pro 或 Kimi 模型。


2. **Context 上下文减肥**：
* 不要在对话框里无脑粘贴 100MB 的 Raw Log 文件！使用本地脚本先提取关键 Error Line，再喂给大模型。


3. **善用 `AGENTS.md**`：
* 将通用的背景知识写入 `AGENTS.md`，避免每次对话重复输入几百字的人设提示词，节省 Input Token。



---

## 模块二：高级功能配置与效率飞跃

### 1. 打造专属于 TD Testing 的 `~/.cimi/cimicode/AGENTS.md`

`AGENTS.md` 是全局上下文配置文件，大模型每次响应前都会优先读取它。

#### 为什么加入岗位与行业背景能优化理解？

* **消歧义**：在半导体领域，`Bump`、`Probe Card`、`Yield`、`Pre-DC`、`SECS/GEM` 都有极强的专业特定含义。如果不配置背景，大模型可能会按通用软件或物理学概念理解，生成无用答案。

#### `AGENTS.md` 配置模板示例：

```markdown
# Role & Background
你是一个半导体先进封装（Advanced Packaging）TD Testing 部门的高级测试工程师与自动化专家。

# Terminology & Context
- Pre-DC: 指 Wafer 减薄/划片/键合前的 Direct Current（直流）电性测试。
- 机台与自动化: 熟知 SECS/GEM 协议、Prober（探针台）、Tester（测试机）的通讯机制。
- 业务重点: 关注测试 Flow 优化、机台报错排查、良率溯源（Yield Traceability）与 CPK 分析。

# Coding & Response Style
- 优先使用 Python 进行数据分析与自动化脚本编写。
- 代码必须包含健壮的异常处理（防止机台通讯中断导致卡死）。
- 回答保持专业、简洁、结果导向，直接给出解决方案。

```

---

### 2. 安装与使用特色 Skill：`Grill-Me` 与 `Web Search`

#### Skill A: `Grill-Me`（小白与复杂需求的救星）

* **痛点**：很多新工程师不知道如何给大模型提需求，导致模型猜错意图、重复修改。
* **原理**：安装 `grill_me` skill 后，大模型不会立刻去写代码或给方案，而是像“严厉的面试官”一样反问你 3~5 个关键细节，直到把需求厘清。
* **安装与使用**：
```bash
# 在 CIMICode 中安装/启用技能
/install-skill grill-me

```


* **使用效果**：
* *工程师*：“帮我写一个 Pre-DC 良率分析脚本。”
* *Grill-Me 触发反问*：
1. 你的数据源是什么格式（CSV、Excel 还是 SQLite）？
2. 良率计算需要剔除 Dummy Die 吗？
3. 异常良率阈值设为多少（例如 < 95% 报警）？





#### Skill B: `Web Search`（获取实时资料）

* **痛点**：大模型训练数据存在截止日期，无法得知最新的开源工具或特定厂商最新的 API 文档。
* **安装与使用**：
```bash
/install-skill web-search

```


* **典型场景**：
> “使用 web search 查询最新的 Python `pysecsgem` 库的配置示例，并帮我集成到现有的探针台监听程序中。”



---

### 3. Plan-Build 深度工作流（加速项目开发）

```
[ 用户需求 ] ──> 【 Plan 模式 】: 拆解 Task / 生成架构图 / 评估风险 (不消耗写权限)
                       │
                 (工程师确认 Plan)
                       │
                       ▼
                 【 Build 模式 】: 自动创建文件 / 逐行写入代码 / 运行测试 (自动落地)

```

1. **Plan 阶段（思考与拆解）**：
* 输入复杂需求（如“重构测试数据解析模块”），让大模型在 Plan 模式下生成 `TODO.md`，列出修改步骤和涉及的文件。
* **优势**：在不破坏任何代码的前提下，审核大模型的思路是否正确。


2. **Build 阶段（自动执行）**：
* 确认 Plan 无误后，切换至 Build 模式，大模型将自动创建文件、修改代码并运行单元测试。



---

### 4. 权限与确认解锁：减少对话中断，实现全自动化

* **痛点**：默认模式下，大模型每次创建文件、执行 Bash 命令都需要用户手动点击 "Approve"，极度影响效率。
* **高级配置**：**授予大模型全自动执行权限（Auto-Approve / Unrestricted Mode）**。
* **应用场景**：在独立的 Git 分支或沙盒环境中，允许大模型连续跑 Shell 命令、安装依赖、排查报错，实现真正无干预的“无人值守开发”。
* ⚠️ **安全红线**：涉及产线机台数据库删除、线上配置文件修改时，严禁开启全局免确认！



---

## 模块三：高级业务落地实操（TD Testing 专属）

### 1. 为什么“任务越具体，效果越好”？（Prompt 黄金法则）

* **模糊任务（效果差）**：“帮我排查这个 Pre-DC 测试机报错。”（模型不知道机台型号、Log 位置、测试项）。
* **具体任务（效果极佳）**：
* **上下文**：Accretech 探针台在执行 Pre-DC 电性测试时报 `ERR-2041 Contact Resistance Exceeded`。
* **目标**：分析 `log_20260726.txt` 中发生 ERR-2041 前后的 Pin 针坐标。
* **约束**：输出一个 Python 散点图绘制脚本，标出高阻异常的 Die 坐标分布。



### 2. 复杂任务实操：输入测试数据并自动设计实验 (DOE)

* **步骤**：
1. 将历史机台测试数据（如不同 Pin 压、不同针尖清洗频次下的 Contact R 数据）存入项目目录。
2. 让大模型读取数据并识别模式。
3. 指令：“基于 `historical_rc.csv` 中的数据，请使用 Taguchi（田口）方法设计一个 9 组实验的 DOE 矩阵，优化参数为：Overdrive (um)、Clean Count、Soak Time，目标是降低 Rc 的标准差。”



### 3. 让大模型进行行业技术调研与头脑风暴

* **场景**：探针台针尖磨损导致的虚焊/测量不准问题（False Contact Failure）。
* **指令**：
> “请作为半导体测试专家，针对 3D 堆叠先进封装（如 CoWoS / HBM）的 Pre-DC 探针测试，检索并整理当前工业界解决 Micro-bump 探针损伤和接触电阻不稳定的主流方案，列出 5 个头脑风暴方向，并对比各自的利弊与引入成本。”



---

# 总结与下一步 Action Items（用于汇报结尾）

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           团队大模型落地路线图                            │
├─────────────────────────────────────────────────────────────────────────┤
│ 1. 权限开通    │ 全员申请 CIMICode Coding 权限，安装桌面版与 TUI           │
│ 2. 配置标准化  │ 统一导入部门级 AGENTS.md (预置 Pre-DC & 封装测试上下文)    │
│ 3. 严格控本    │ 日常使用 Flash 模型，极复杂场景经评估后调用 Pro/Kimi      │
│ 4. 技能赋能    │ 组内推广 Grill-Me 与 Plan-Build 工作流，提升提问质量      │
└─────────────────────────────────────────────────────────────────────────┘

```
