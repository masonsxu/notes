# AGENTS.md · notes 项目规范

项目级规则，优先级高于全局规则（`~/.zcode/AGENTS.md`），用户当次指令最高。每条规则附带原因；不满足"有明确原因"的规则应被删除。

## 敏感信息：落地主体身份（2026-09-19 起）

背景：`ref/hbm3e-hbm4e/` 材料的落地主体是一家具体公司。2026-09-19 曾因主体身份信息（名称/地址/供应链关联/报道出处）被写入材料并提交推送，导致 git 历史与远程仓库删除重建。自即日起按本节执行。

1. **身份信息只存在于对话**。落地主体及任何客户/供应商/合作方的名称、简称、成立与注册信息、地址、法人、规模、专利号、供应链关联、报道出处（含可反查主体的媒体名+日期组合与 URL）——只允许在对话沟通中出现；NEVER 写入任何文件（材料/代码/注释/README）与 git 提交信息、分支名、tag。
   原因：材料会入库、推送、分享，身份一旦入库即脱离控制；泄露不可逆，多写一个名字没有任何收益。

2. **材料用泛称**。文档与材料中一律写"公司（封测/OSAT 定位）"；分析落盘所依赖的最小假设 = 封测厂定位 + 产线爬坡期，超出该层的身份细节一律不入盘。现有脱敏基线见 `ref/hbm3e-hbm4e/数据核对与落地评估报告.md` §4.1。
   原因：落地评估只需"封测厂定位"这一层假设即可成立，更细的身份信息对结论无增量。

3. **落盘前先脱敏**。调研结论引用来源时同步脱敏：出处写"公开报道（出处留对话）"，不写可反查主体的检索词组合；需要回溯时由助手在对话中给出原始出处。
   原因：来源出处是最容易被忽略的二次泄露路径（媒体名+日期即可反查主体）。

4. **交付前自检**。涉及 `ref/hbm3e-hbm4e/`（或任何含主体信息的材料）的任务收尾时，助手必须主动对主体名称及全部关联识别词做全库 grep（材料/代码/注释/提交信息），零残留并报告结果才算交付。
   原因：依赖自觉必然遗漏，机械化检查才能兜底。

5. **已暴露按已暴露处理**。若身份信息已写入并推送过，删除仓库/远程不等于清除（远程缓存、搜索快照、fork 可能留存），须按"已暴露"提醒用户处置，NEVER 默认"删了就没事"。

6. **拿不准就问**。可缩小主体范围的边缘表述（如"服务国产 HBM 供应链"这类地域+行业组合）同样按疑似身份信息处理：先在对话中确认，再落盘。
   原因：单条信息无害、组合可识别，边界判断的成本远低于泄露成本。
