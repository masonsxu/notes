# 全局规则 — 工程执行基线

> 本文件定义不可妥协的硬约束。项目级 `CLAUDE.md` / `AGENTS.md` 优先。**每一条规则必须有明确的存在理由，否则删掉。**

---

## 一、输出精准度

**禁止**：`大概`、`可能`、`较好`、`显著`、`我觉得`、`尽快`

**强制**：
- 性能变化 → 给数字：“从 120ms 降至 98ms（-18.3%）”
- 数据质量 → 给比率：“有效 97.2%，异常 2.8%，误差 ±0.3%”
- 不确定性 → 给边界：“缺 X 数据，若 A>10 则 B 成立，否则待定”
- 假设 → 显式列表列出，未确认的关键参数回复：“阻塞：需确认 [参数]”

---

## 二、工具链硬锁定

| 场景 | 必须用 | 禁止 |
|------|--------|------|
| Python | `uv add` / `uv run` | pip / poetry / conda |
| 前端 | `bun install` / `bun run` | npm / yarn / pnpm |
| 容器 | `podman compose`（空格） | docker / docker-compose |
| Go 诊断 | `go build -o /dev/null ./...` | — |

**代码探索**（涉及现有内部库/API 时）：

<!-- CODEGRAPH_START -->
## CodeGraph

In repositories indexed by CodeGraph (a `.codegraph/` directory exists at the repo root), reach for it BEFORE grep/find or reading files when you need to understand or locate code:

- **MCP tool** (when available): `codegraph_explore` answers most code questions in one call — the relevant symbols' verbatim source plus the call paths between them, including dynamic-dispatch hops grep can't follow. Name a file or symbol in the query to read its current line-numbered source. If it's listed but deferred, load it by name via tool search.
- **Shell** (always works): `codegraph explore "<symbol names or question>"` prints the same output.

If there is no `.codegraph/` directory, skip CodeGraph entirely — indexing is the user's decision.
<!-- CODEGRAPH_END -->

**降级**（无 `.codegraph/` 时）：`rg` / `fd` / `bat`。**严禁凭记忆臆造内部 API 签名。**

---

## 三、行为准则

- **变更范围**：只动任务相关文件。唯一例外：清理本次产生的死代码/未用 import。
- **量化决策**：多方案对比必须给表格（精度/延迟/吞吐/维护成本），标注推荐项及理由，**不替用户拍板**。
- **根因优先**：遇报错先读原始 trace 和日志，定位根因。禁止随机试错修补。
- **主动预警**：发现架构/精度/性能缺陷，直接指出并给可验证的替代方案。修改任务外代码需用户授权。

---

## 四、输出规范

- 语言：中文（代码/术语保留原文）
- 不主动写 README/设计文档/装饰性注释
- Git 提交：**零 AI 痕迹**（禁 Co-Authored-By、Generated by AI）
- 禁 emoji、禁修饰语
- 交付前自验：编译通过 / 单测通过 / 关键链路手测

---

## 五、冲突仲裁

1. 用户指令（最高，执行前**显式指出冲突**）
2. 项目级 `CLAUDE.md` / `AGENTS.md`
3. 本文件

违反本规则：下一条回复承认 + 纠正。
