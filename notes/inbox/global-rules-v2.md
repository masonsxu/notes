# 全局规则方案 v2 — 渐进式披露（AGENTS.md + Skills）

> 状态：已部署（2025-09-18）。本文件是完整快照，由已部署文件直接拼装生成，可用于跨机再部署。
> 取代：`global-rules.md`（v1，单体中文 + PowerShell 基线，已删除，原因见 [README](./)）。

## 1. 架构与原理

| 层 | 内容 | 加载时机 |
|---|---|---|
| 全局 `AGENTS.md`（45 行，英文） | Shell 约束、核心行为、冲突仲裁、skill 触发表 | 每次会话全量注入 |
| `AGENTS.zh.md`（45 行，中文） | 中文对照 | 不注入，仅供人读 |
| `~/.agents/skills/<name>/SKILL.md`（6 个） | 任务规则：工具链 / CodeGraph / 精准度 / 设计编排 | 触发条件命中时按需加载 |

原理：启动时只注入 skill 的 `name` + `description`（每个约 40 token），任务命中才读完整 `SKILL.md`（渐进式披露）。常驻上下文从 v1 的约 130 行降到 45 行，任务无关规则不再污染上下文。

## 2. 部署位置

| 宿主 | AGENTS.md | skills 根目录 |
|---|---|---|
| pi（macOS，已部署） | `~/.pi/agent/AGENTS.md`（权威）+ 同目录 `AGENTS.zh.md` | `~/.agents/skills/`（pi 原生加载）；第三方 skill 在 `~/.pi/agent/skills/` |
| opencode（Windows，待部署） | `%USERPROFILE%\.config\opencode\AGENTS.md` | `%USERPROFILE%\.agents\skills\`（与 `npx skills` 托管一致） |

优先级：用户指令 > 项目级 `CLAUDE.md` / `AGENTS.md` > 全局 `AGENTS.md`。规则漂移时以 pi 上的已部署文件为准，本快照随之重新生成。

## 3. 全局 AGENTS.md（英文权威版，45 行）

```markdown
# Global Rules

> Hard constraints. Project-level `CLAUDE.md` / `AGENTS.md` override. User instructions are highest.
> `AGENTS.zh.md` is the Chinese mirror for human reading; this file wins on drift.
> Every rule MUST have a clear reason to exist; otherwise delete it.

## Shell

- All shell commands follow POSIX/bash semantics.
- On Windows hosts: Git Bash only (`bash.exe`); NEVER PowerShell or `cmd`; UTF-8 I/O.
- Containers: MUST `podman compose` (space); NEVER `docker` / `docker-compose`.

## Core Behavior

- Reply language: Chinese; code/terms stay in original form.
- MUST quantify: performance (numbers), data quality (ratios), uncertainty (boundaries); list assumptions explicitly; unconfirmed critical parameter → `Blocked: need confirmation on [param]`.
- MUST NOT use vague qualifiers: 大概, 可能, 较好, 显著, 我觉得, 尽快.
- MUST delete AI filler modifiers (deletion test + examples: load `precision-detail`).
- MUST only modify task-relevant files. Sole exception: dead code/unused imports created by this change.
- On errors: read the raw trace and logs first; MUST NOT random-trial patch.
- Multi-option decisions: MUST give a comparison table (accuracy/latency/throughput/maintenance cost) + recommendation + rationale; MUST NOT decide for the user.
- MUST NOT proactively write README/design docs/decorative comments.
- NEVER generate `.pptx` (including `python-pptx` scripts); deliver `Markdown`/`HTML` content only.
- Git commits: ZERO AI traces (no `Co-Authored-By`, `Generated by AI`).
- NO emoji.
- Before delivery: build passes / tests pass / critical path manually checked.

## Conflict Arbitration

1. User instruction (highest; state conflicts before executing)
2. Project-level `CLAUDE.md` / `AGENTS.md`
3. This file

## Skills

Load the matching skill when its trigger condition is met; full rules live in `~/.agents/skills/<name>/SKILL.md`.

| Trigger | Skill |
|---|---|
| Python code, dependencies, formatting | `python-toolchain` |
| Frontend / JavaScript / TypeScript | `frontend-toolchain` |
| Go code, build errors | `go-diagnostics` |
| `.codegraph/` at repo root; prefer it before grep/find | `codegraph` |
| Reports, design docs, meeting notes, commit messages | `precision-detail` |
| UI tasks using frontend-design + ui-ux-pro-max | `design-orchestration` |
```

## 4. AGENTS.zh.md（中文对照版，45 行）

```markdown
# 全局规则（中文对照版）

> 硬约束。项目级 `CLAUDE.md` / `AGENTS.md` 可覆盖；用户指令最高。
> 本文件是 `AGENTS.md` 的中文对照版，供人阅读；不一致时以 `AGENTS.md` 为准。
> 每一条规则必须有明确的存在理由，否则删掉。

## Shell

- 所有 shell 命令按 POSIX/bash 语义执行。
- Windows 宿主：只用 Git Bash（`bash.exe`）；禁 PowerShell / `cmd`；I/O 用 UTF-8。
- 容器：必须 `podman compose`（空格）；禁 `docker` / `docker-compose`。

## 核心行为

- 回复语言：中文；代码/术语保留原文。
- 必须量化：性能（数字）、数据质量（比率）、不确定性（边界）；假设显式列出；未确认的关键参数 → `阻塞：需确认 [参数]`。
- 禁模糊限定词：大概、可能、较好、显著、我觉得、尽快。
- 必须删除 AI 无信息修饰语（删除测试 + 范例：加载 `precision-detail`）。
- 只动任务相关文件。唯一例外：清理本次产生的死代码/未用 import。
- 遇报错先读原始 trace 和日志；禁止随机试错修补。
- 多方案决策：必须给对比表（精度/延迟/吞吐/维护成本）+ 推荐项 + 理由；不替用户拍板。
- 不主动写 README/设计文档/装饰性注释。
- 禁生成 `.pptx`（含 `python-pptx` 脚本）；只交付 `Markdown`/`HTML` 内容稿。
- Git 提交：零 AI 痕迹（禁 `Co-Authored-By`、`Generated by AI`）。
- 禁 emoji。
- 交付前自验：编译通过 / 单测通过 / 关键链路手测。

## 冲突仲裁

1. 用户指令（最高；执行前指出冲突）
2. 项目级 `CLAUDE.md` / `AGENTS.md`
3. 本文件

## Skills

任务命中触发条件时加载对应 skill；完整规则在 `~/.agents/skills/<name>/SKILL.md`。

| 触发条件 | Skill |
|---|---|
| Python 代码、依赖、格式化 | `python-toolchain` |
| 前端 / JavaScript / TypeScript | `frontend-toolchain` |
| Go 代码、构建报错 | `go-diagnostics` |
| 仓库根有 `.codegraph/`；优先于 grep/find | `codegraph` |
| 周报、设计文档、会议纪要、commit message | `precision-detail` |
| 使用 frontend-design + ui-ux-pro-max 的 UI 任务 | `design-orchestration` |
```

## 5. Skills（6 个，均 ≤ 50 行）

### 5.1 `python-toolchain`

```markdown
---
name: python-toolchain
description: Use when the task involves Python code, dependencies, environments, or formatting. Enforces uv and ruff; forbids pip/poetry/conda.
---

# Python Toolchain

- MUST use `uv add` / `uv run` for dependencies and execution. MUST NOT use `pip` / `poetry` / `conda`.
- After Python changes, run `ruff format`.
- Run `ruff check --fix` only if the project has `[tool.ruff]` config (default rules can break `star import`s).
```
### 5.2 `frontend-toolchain`

```markdown
---
name: frontend-toolchain
description: Use when the task involves frontend code, JavaScript, TypeScript, package installation, or package scripts. Enforces bun; forbids npm/yarn/pnpm.
---

# Frontend Toolchain

- MUST use `bun install` / `bun run`. MUST NOT use `npm` / `yarn` / `pnpm`.
```
### 5.3 `go-diagnostics`

```markdown
---
name: go-diagnostics
description: Use when the task involves Go code or Go build/compile errors. Enforces build-first diagnostics.
---

# Go Diagnostics

- Diagnose with `go build -o /dev/null ./...` before any fix; read the full compiler output first.
```
### 5.4 `codegraph`

```markdown
---
name: codegraph
description: Use when a `.codegraph/` directory exists at the repo root and the task needs to understand or locate code. Prefer it BEFORE grep/find or reading files.
---

# CodeGraph

- MCP tool (when available): `codegraph_explore` answers most code questions in one call — the relevant symbols' verbatim source plus the call paths between them, including dynamic-dispatch hops grep can't follow. Name a file or symbol in the query to read its current line-numbered source. If it's listed but deferred, load it by name via tool search.
- Shell (always works): `codegraph explore "<symbol names or question>"` prints the same output.
- If there is no `.codegraph/` directory, skip CodeGraph entirely — indexing is the user's decision.
- Fallback tools: `rg` / `fd` / `bat`. NEVER invent internal API signatures from memory.
```
### 5.5 `precision-detail`

```markdown
---
name: precision-detail
description: Use when writing weekly reports, design docs, meeting notes, commit messages, or any long-form text. Provides the AI filler modifier deletion test, examples, and self-check.
---

# Output Precision Detail

Filler modifiers are the main source of the "AI flavor". If deleting a modifier leaves the meaning unchanged, or weakens a meaning that a number/fact should carry, delete or replace it.

## Deletion Test

- Delete the word; meaning unchanged → DELETE
- Delete the word; meaning weakens → REPLACE with a number/fact, NEVER a synonym
- Meaning depends on the word → KEEP

## Examples

| Category | Before → After |
|---|---|
| Decorative adverbs | 针对性适配 → 适配 · 实测核对 → 核对 · 自行适配 → 适配 · 进一步优化 → 优化 |
| Decorative adjectives | 高级信息 → 信息 · 独立阻塞 → 阻塞 · 深度分析 → 分析 · 有效方案 → 方案 |
| Vague qualifiers | 初步了解 → 了解 · 直接复用 → 复用 · 基本完成 → 完成（或给实际进度%） |
| Hollow descriptions | 响应不及时 → 8 个月才响应 · 性能很差 → 从 120ms 升至 950ms |
| Intensity words | 严重滞后 → 滞后 3 天 · 明显改进 → 从 72% 提至 88% · 大幅提升 → +16pp |

## Self-Check

Before delivery, run the deletion test on every sentence; remove at least one class of filler. If a sentence turns hollow after deletion, it needed numbers/facts, not synonyms.

## Scope

Weekly reports, design docs, meeting notes, PPT body, commit messages. Code comments/identifiers are exempt (naming follows each language's spec).
```
### 5.6 `design-orchestration`

```markdown
---
name: design-orchestration
description: Use when building or reviewing UI with the frontend-design and ui-ux-pro-max skills. Defines which skill leads on which axis, the three-stage pipeline, and the delivery gate.
---

# Design Skill Orchestration (frontend-design + ui-ux-pro-max)

Both skills exist in this environment. Their trigger conditions overlap, and on the color/typography/style axes they assert opposite claims (derived vs database lookup); without orchestration rules the output direction is unstable.

## Division of Labor

| Skill | Responsibility | Use case |
|---|---|---|
| frontend-design | Visual direction distinctiveness: token plan → anti-template self-review → code | Brand-new pages/sites, tasks needing a visual identity |
| ui-ux-pro-max | System completeness: UX guidelines, interaction patterns, delivery checklist | Component development, UX/a11y review, delivery gate |

## Pipeline (three stages for large new-UI tasks)

1. Set direction: frontend-design leads (4–6 color tokens + font pairing + signature element + self-review)
2. Consult rules: ui-ux-pro-max retrieves patterns (UX guidelines, component specs, `--stack` guidance); it MUST NOT lead visual direction
3. Delivery gate: ui-ux-pro-max PRE-DELIVERY CHECKLIST (contrast 4.5:1, visible keyboard focus, `prefers-reduced-motion`, 375/768/1024/1440px breakpoints) + frontend-design restraint self-review (remove one decoration)

## Conflict-Axis Arbitration

On the color/typography/style axes exactly one skill leads at a time:
- New project: frontend-design sets direction.
- Existing design system: ui-ux-pro-max supplies specs.
- Project-level rules already lock fonts/colors: both skills' recommendations yield to project-level `AGENTS.md`.

## Retrieval Script Essentials (ui-ux-pro-max)

- Full-path invocation, no cwd dependency. Adjust `<skills-root>` to where the skill is installed (`~/.agents/skills/` or `~/.pi/agent/skills/`):
  `uv run python <skills-root>/ui-ux-pro-max/scripts/search.py "<query>" --domain <style|color|ux|typography|chart|...> [--stack <react|shadcn|vue|...>]`
- The `ux` domain matches guideline keywords (`keyboard` / `contrast` / `forms`), not product keywords (`portfolio`).
- On 0 results, explicitly declare "no database match"; NEVER silently degrade or fabricate.
```
```

## 6. 关键决策记录

- **英文权威 + 中文单独 `.zh.md`**：规则是给模型看的，`MUST` / `NEVER` 理解更一致；逐条中英对照是负收益（token 翻倍、双语噪音、维护漂移）
- **任务规则下沉 skill**：Python/前端/Go 工具链、CodeGraph、无信息修饰语范例、设计编排只在对应任务中相关，按需加载
- **CodeGraph 标记块下沉**：`codegraph init --help` 确认 CLI 无 AGENTS.md 注入子命令，`<!-- CODEGRAPH_START -->` 块是纯认知文本；pi 中 `codegraph_explore` 可用性取决于 MCP/扩展配置，与该块无关
- **Windows 用 Git Bash 而非 pwsh**：opencode 存在 `shell` 配置被忽略回退 PowerShell 5.1、中文 GBK 乱码、bash 与模型训练数据更匹配三个问题；适配步骤见 [README](./)
- **v1 的 Windows 特有写法已废弃**：`go build -o NUL` → `/dev/null`（Git Bash POSIX）；裸 `python3` → `uv run python`（跨平台且符合执行锁）；`%USERPROFILE%` PowerShell 路径 → `~` POSIX 路径

## 7. 验证清单

- [x] `AGENTS.md` ≤ 60 行（实测 45）
- [x] `SKILL.md` ≤ 50 行（最大 35）
- [x] description 以 "Use when" 开头（6/6）；name 与目录名一致、全小写连字符（6/6）
- [x] `CODEGRAPH_START/END` 标记已移出全局文件
- [ ] 触发实测（下次会话）：Python 任务 → `python-toolchain` 加载；Go 任务 → 无误触发；token 消耗对比
