# 《大模型成本优化与高级功能配置》原始材料

> **目标受众**：老板、组内资深工程师、TL
> **核心目的**：解决"用不起"、"用不好"和"效率低"三大痛点，实现组内大模型使用降本增效
> **使用方式**：本文档为 PPT 原始材料，包含所有需要呈现的内容要点和演示素材

---

## 一、Token 成本现状与分析

### 1.1 公司 Token 成本对比

| 模型 | 公司内部 Token 算力单价 | 相比公网倍率 | 推荐使用场景 |
| --- | --- | --- | --- |
| **DeepSeek v4 Flash** | **极低** | **基准** | **日常 80% 场景**：简单代码编写、Log 解析、格式转换、文档整理 |
| **DeepSeek v4 Pro** | 极高 | ~5x | **复杂逻辑推理**：复杂 Bug 深度排查、测试架构设计、算法推导 |
| **GLM 5.1 / 5.2** | 高 | ~5x | **长文本与中文公文**：长篇 SOP 编写、跨文件复杂整理 |
| **Kimi 2.7** | 高 | ~5x | **超长上下文解析**：超大型 Log 校验、海量文档对比 |

> ⚠️ **重要提醒**：除 Flash 模型外，高阶模型成本比公网贵 5 倍左右！如果不加限制地全局使用 Pro/GLM 模型，单人月度账单将极高。

### 1.2 成本构成分析

大模型使用成本主要由以下因素决定：

```
总成本 = 输入 Token × 输入单价 + 输出 Token × 输出单价

其中：
- 输入 Token = 系统 Prompt + AGENTS.md + 对话历史 + 用户输入
- 输出 Token = 大模型的回复内容
```

**成本影响因素**：
- 对话轮次越多，累积的历史上下文越长，输入 Token 越多
- 大模型回复越详细，输出 Token 越多
- 选择的模型单价不同，成本差异巨大

### 1.3 模型选择策略

| 任务类型 | 推荐模型 | 理由 |
| --- | --- | --- |
| 简单文本整理 | Flash | 速度优先，成本最低 |
| 代码草稿编写 | Flash | 快速迭代，成本可控 |
| Log 解析（简单） | Flash | 格式转换类任务，Flash 足够 |
| 复杂 Bug 排查 | Pro | 需要深度推理和多步分析 |
| 架构设计 | Pro | 需要全局思考和权衡 |
| 长篇 SOP 编写 | GLM | 长文本生成能力强 |
| 海量文档对比 | Kimi | 超长上下文支持 |

---

## 二、降本增效"组合拳"法则

### 2.1 法则一：80/20 梯队模型策略

```
日常任务 (80%) ──> DeepSeek v4 Flash（低成本、快速响应）
                    │
                    ├── 简单代码编写
                    ├── 文本格式转换
                    ├── 邮件草稿生成
                    └── 基础 Log 解析

复杂任务 (20%) ──> Pro / GLM / Kimi（高成本、深度推理）
                    │
                    ├── 复杂逻辑推理
                    ├── 架构设计
                    ├── 长篇文档生成
                    └── 海量数据对比
```

**实施要点**：
- 默认使用 Flash 模型
- 仅在 Flash 模型遇到瓶颈（如多次修改代码未解决、逻辑推理死循环）时，才切换至高阶模型
- 建立团队共识：什么程度的任务才值得使用高阶模型

### 2.2 法则二：Context 上下文减肥

**问题**：不要在对话框里无脑粘贴 100MB 的 Raw Log 文件！

**解决方案**：
1. **预处理脚本**：先用脚本提取关键 Error Line，再喂给大模型
2. **分段处理**：大型文件分段处理，每次只给大模型需要分析的部分
3. **结构化输入**：将原始数据整理成表格或 JSON 格式，减少大模型解析负担

**示例**：
```python
# 不好的做法：直接把整个 Log 文件粘贴给大模型
# 好的做法：先用脚本提取关键信息
grep "ERROR\|WARN" large_log.txt > filtered_errors.txt
# 然后只把 filtered_errors.txt 给大模型
```

### 2.3 法则三：善用 AGENTS.md

**问题**：每次对话都重复输入几百字的人设提示词，浪费 Input Token。

**解决方案**：
- 将通用的背景知识写入 `AGENTS.md`
- 大模型每次响应前都会自动读取
- 一次配置，永久生效

**成本对比**：
- 每次手动输入背景：~500 tokens × 每天 20 次 = 10,000 tokens/天
- 使用 AGENTS.md：0 tokens（已预加载）
- **月节省**：~300,000 tokens/人

### 2.4 法则四：对话复用与会话管理

**问题**：每次新对话都要重新输入上下文，浪费时间和 Token。

**解决方案**：
1. **保存常用对话模板**：将常用的指令模板保存，下次直接复用
2. **利用会话历史**：在同一会话中继续之前的讨论，避免重复输入
3. **及时结束无用会话**：如果发现方向错误，及时结束会话重新开始

---

## 三、AGENTS.md 高级配置

### 3.1 为什么 AGENTS.md 能优化理解？

#### 消歧义作用

半导体领域术语有专业特定含义，如果不配置背景，大模型可能按通用概念理解：

| 术语 | 通用理解 | 半导体专业理解 |
| --- | --- | --- |
| Bump | 碰撞、撞击 | 微凸块，用于芯片互连 |
| Probe Card | 探测卡 | 探针卡，用于晶圆测试 |
| Yield | 产量、收益 | 良率，合格品比例 |
| Pre-DC | 预直流 | 减薄/划片/键合前的直流电性测试 |
| SECS/GEM | 无 | 半导体设备通讯标准协议 |

#### 提升效率

- 避免每次对话重复输入背景信息
- 大模型可以从第一次响应就理解专业术语
- 出错率显著降低

### 3.2 AGENTS.md 完整配置模板

```markdown
# Role & Background

你是一个半导体先进封装 TD Testing 部门的高级测试工程师与自动化专家。
你的职责包括：
- 测试 Flow 设计与优化
- 机台自动化程序开发
- 测试数据分析与可视化
- 机台报错排查与解决
- 良率溯源与 DOE 实验设计

# Terminology & Context

## 核心业务术语
- Pre-DC: Wafer 减薄/划片/键合前的直流电性测试
- CPK: 过程能力指数，衡量制程稳定性
- Yield: 良率，合格品占总数的比例
- DOE: 实验设计，用于优化工艺参数
- SPC: 统计过程控制

## 机台与自动化
- SECS/GEM: 半导体设备通讯标准协议
- Prober: 探针台，用于晶圆级测试
- Tester: 测试机，执行电性测试
- Handler: 分选机，用于成品测试

## 数据格式
- 常见数据格式: CSV, Excel, SQLite, JSON
- 日志格式: 纯文本 Log 文件，包含时间戳、设备ID、错误码等

# Coding & Response Style

## 编程偏好
- 优先使用 Python 进行数据分析与自动化脚本编写
- 代码必须包含健壮的异常处理（防止机台通讯中断导致卡死）
- 优先使用 pandas 进行数据处理
- 图表使用 matplotlib 或 plotly

## 回答风格
- 保持专业、简洁、结果导向
- 直接给出解决方案，避免冗长的铺垫
- 代码示例要可直接运行
- 复杂问题分步骤解释

# Safety & Constraints

- 涉及产线机台操作时，必须先确认安全步骤
- 代码修改前先备份
- 不要直接删除文件，先移动到备份目录
```

### 3.3 分项目配置

除了全局 AGENTS.md，还可以在项目目录下创建项目级配置：

```
~/.cimi/cimicode/AGENTS.md          # 全局配置（对所有项目生效）
<项目目录>/AGENTS.md                  # 项目级配置（仅对该项目生效）
```

**项目级配置示例**（用于特定测试项目）：
```markdown
# Project-Specific Context

## 项目信息
- 项目名称: XXX 产品 Pre-DC 测试
- 测试机型: Accretech Prober + Advantest Tester
- 测试项目: Contact Resistance, Leakage, Continuity

## 数据目录结构
- ./logs/: 原始 Log 文件
- ./data/: 处理后的测试数据
- ./scripts/: 自动化脚本
- ./reports/: 生成的报告

## 特殊要求
- 所有脚本必须支持 UTF-8 编码
- 时间戳统一使用 UTC+8
```

---

## 四、Plan-Build 高级工作流

### 4.1 工作流程详解

```
[ 用户需求 ] ──> 【 Plan 模式 】
                       │
                       ├── 1. 理解需求
                       ├── 2. 拆解任务
                       ├── 3. 设计架构
                       ├── 4. 评估风险
                       ├── 5. 生成 TODO.md
                       │
                 (工程师审核 Plan)
                       │
                       ├── 通过 ──> 【 Build 模式 】
                       │                │
                       │                ├── 自动创建文件
                       │                ├── 逐行写入代码
                       │                ├── 运行测试
                       │                └── 生成报告
                       │
                       └── 修改 ──> 重新进入 Plan 模式
```

### 4.2 Plan 模式使用技巧

**输入复杂需求**：
```
进入 Plan 模式，帮我规划一个测试数据自动化分析平台的架构，
需求如下：
1. 支持多种数据源（CSV, Excel, SQLite）
2. 自动生成可视化报告（CPK 趋势图、Yield 分布图）
3. 支持历史数据对比
4. 异常数据自动报警
5. 输出 HTML 和 PDF 两种格式的报告
```

**Plan 输出示例**：
```markdown
# 测试数据自动化分析平台 - 实施计划

## 架构设计
- 数据层: pandas + SQLAlchemy
- 处理层: 数据清洗、统计计算
- 展示层: matplotlib/plotly 图表生成
- 输出层: HTML/PDF 报告生成

## 任务拆解
### Phase 1: 基础框架 (预计 2 天)
- [ ] 创建项目目录结构
- [ ] 实现数据加载模块
- [ ] 实现基础统计计算

### Phase 2: 可视化 (预计 2 天)
- [ ] 实现 CPK 趋势图
- [ ] 实现 Yield 分布图
- [ ] 实现异常数据标记

### Phase 3: 报告生成 (预计 1 天)
- [ ] 实现 HTML 报告模板
- [ ] 实现 PDF 导出功能
- [ ] 添加历史对比功能

## 风险评估
- 数据格式不一致 → 需要数据清洗层
- 大数据量性能 → 考虑分块处理
- 图表样式统一 → 使用模板配置

## 涉及文件
- src/data_loader.py
- src/analyzer.py
- src/visualizer.py
- src/report_generator.py
- config/report_template.html
```

### 4.3 Build 模式执行

**确认 Plan 后**：
```
Plan 已确认，现在进入 Build 模式，开始执行。请按照计划逐步实施。
```

**Build 执行过程**：
- 大模型自动创建目录结构
- 逐个创建文件并写入代码
- 运行测试验证功能
- 生成最终报告

### 4.4 加速项目开发的技巧

#### 1. 并行任务拆解
```
将这个项目拆解为 3 个并行任务：
1. 数据处理模块（独立）
2. 可视化模块（独立）
3. 报告生成模块（依赖前两个）

请分别制定每个模块的 Plan。
```

#### 2. 增量式开发
```
先实现最小可用版本（MVP），只包含 CSV 数据加载和基础统计，
后续再逐步添加可视化和报告功能。
```

#### 3. 代码复用
```
参考项目中已有的 data_loader.py 模块，实现一个类似结构的
analyzer.py 模块，保持代码风格一致。
```

---

## 五、Skill 高级配置

### 5.1 Grill-Me Skill 深度使用

#### 为什么需要需求澄清？

**常见问题**：
- 工程师说"帮我分析数据"，但没说分析什么、输出什么格式
- 工程师说"写个脚本"，但没说输入输出、异常处理要求
- 工程师说"优化性能"，但没说优化到什么程度

#### Grill-Me 工作原理

```
用户输入 ──> Grill-Me 分析 ──> 反问 3~5 个关键问题
                                      │
                            用户回答关键问题
                                      │
                                      ▼
                         大模型获得完整需求 ──> 生成准确代码
```

#### Grill-Me 反问示例

**用户输入**：
```
帮我写一个 Log 解析脚本
```

**Grill-Me 反问**：
```
为了准确实现你的需求，请回答以下问题：

1. Log 文件的格式是什么？
   - 纯文本 (.txt)
   - JSON 格式
   - XML 格式
   - 其他格式

2. 需要提取哪些关键信息？
   - 错误码 (Error Code)
   - 时间戳
   - 设备 ID
   - 测试结果
   - 其他字段

3. 输出格式要求？
   - CSV 表格
   - Excel 文件
   - JSON 数据
   - Markdown 表格

4. 是否需要统计分析？
   - 错误频次统计
   - 时间分布分析
   - 设备对比分析

5. 是否需要可视化？
   - 生成图表
   - 仅文本报告
```

### 5.2 Web Search Skill 深度使用

#### 应用场景

| 场景 | 使用方式 | 示例 |
| --- | --- | --- |
| 查找最新 API 文档 | 搜索特定库的最新版本 | "搜索 pysecsgem 最新 API 文档" |
| 技术方案调研 | 搜索行业最佳实践 | "搜索半导体测试自动化最佳实践" |
| 问题排查 | 搜索类似问题的解决方案 | "搜索 Prober ERR-2041 错误解决方案" |
| 标准查询 | 搜索行业标准更新 | "搜索 JEDEC 最新标准更新" |

#### Web Search 使用技巧

**精准搜索**：
```
使用 web search 搜索 "Python SECS/GEM 库 pysecsgem 配置示例"，
我需要了解如何配置探针台通讯。
```

**多轮搜索**：
```
第一轮：搜索 pysecsgem 库的基本用法
第二轮：搜索如何处理通讯中断异常
第三轮：搜索如何实现设备状态监控
```

### 5.3 其他实用 Skill

#### Auto-Approve（自动执行）

**配置方式**：
```
/install-skill auto-approve
```

**使用场景**：
- 个人开发环境
- 独立的 Git 分支
- 沙盒测试环境

**安全提醒**：
- ⚠️ 仅在安全环境中使用
- ⚠️ 不要在产线相关系统中开启

#### Code Review（代码审查）

**配置方式**：
```
/install-skill code-review
```

**使用场景**：
- 提交代码前的自动审查
- 检查代码规范和潜在问题
- 生成代码审查报告

---

## 六、Prompt 工程：任务越具体，效果越好

### 6.1 Prompt 黄金法则

**核心原则**：给大模型的信息越具体，输出的结果越准确。

### 6.2 模糊 vs 具体对比

#### 模糊任务（效果差）

```
帮我排查这个测试机报错。
```

**问题**：
- 不知道机台型号
- 不知道 Log 文件位置
- 不知道测试项
- 不知道错误现象

**可能的输出**：通用的排查步骤，不具针对性

#### 具体任务（效果极佳）

```
Accretech 探针台在执行 Pre-DC 电性测试时报 ERR-2041 
Contact Resistance Exceeded 错误。

具体情况：
- 错误发生在 Wafer 第 3 次 Pass 时
- 涉及 Pin 坐标范围: Row A-F, Column 1-12
- 历史 Log 文件在 ./logs/prober_20260726.txt

请帮我：
1. 分析 Log 文件中发生 ERR-2041 前后的 Pin 针坐标
2. 识别高阻异常的 Die 坐标分布
3. 生成一个 Python 散点图脚本，标出异常位置
```

**输出**：针对性的分析脚本，直接可用

### 6.3 结构化 Prompt 模板

```
【背景】
- 项目/场景描述
- 相关技术栈
- 已有条件

【目标】
- 期望实现的功能
- 输出格式要求

【约束】
- 性能要求
- 兼容性要求
- 安全限制

【输入】
- 数据文件位置
- 代码文件位置
- 配置文件位置

【输出】
- 期望的文件格式
- 文件保存位置
- 是否需要测试
```

### 6.4 Prompt 优化技巧

#### 1. 提供示例
```
请参考以下示例格式生成报告：

| 错误码 | 发生次数 | 占比 | 影响设备 |
| --- | --- | --- | --- |
| ERR-2041 | 15 | 30% | Prober-01 |

请按照此格式整理 ERR-2041 的所有相关错误。
```

#### 2. 分步骤指令
```
请按以下步骤处理：
1. 首先读取 ./data/test_results.csv
2. 然后筛选出 Yield < 98% 的 Wafer
3. 接着统计每个 Lot 的异常 Wafer 数量
4. 最后生成 Excel 报告并保存到 ./reports/
```

#### 3. 明确输出格式
```
请生成以下格式的输出：

Python 脚本要求：
- 使用 pandas 读取 CSV
- 使用 matplotlib 生成图表
- 图表保存为 PNG 格式
- 脚本顶部添加注释说明

输出文件：
- analyzer.py（主脚本）
- README.md（使用说明）
```

---

## 七、数据输入与实验设计

### 7.1 如何给大模型输入测试数据

#### 方式一：直接读取项目文件

```
请读取 ./data/rc_data.csv 文件，分析其中的接触电阻数据。
文件包含以下字段：
- Lot_ID: 批次号
- Wafer_ID: 晶圆号
- Die_X, Die_Y: Die 坐标
- Rc: 接触电阻值
- Timestamp: 测试时间
```

#### 方式二：粘贴关键数据

```
以下是最近 3 批次的接触电阻统计数据：

批次 | 样本数 | 平均值 | 标准差 | CPK
--- | --- | --- | --- | ---
Lot-001 | 1000 | 45.2 mΩ | 3.1 | 1.42
Lot-002 | 980 | 48.7 mΩ | 4.5 | 0.98
Lot-003 | 1020 | 43.8 mΩ | 2.8 | 1.56

请分析数据趋势，并设计优化方案。
```

#### 方式三：提供数据文件路径

```
测试数据文件位于：
- 原始数据: /Volumes/Data/test/raw_data.csv
- 处理后数据: /Volumes/Data/test/processed.csv
- 配置文件: /Volumes/Data/test/config.json

请读取这些文件并进行综合分析。
```

### 7.2 基于已有数据设计实验

#### DOE 实验设计流程

```
[ 已有数据 ] ──> [ 数据分析 ] ──> [ 识别问题 ] ──> [ 设计 DOE ]
                                                  │
                                                  ├── 确定因子
                                                  ├── 确定水平
                                                  ├── 选择实验设计类型
                                                  └── 生成实验矩阵
```

#### DOE 设计示例

**输入**：
```
这是我们最近 3 批次的接触电阻数据 rc_data.json。

数据特点：
- 平均接触电阻: 45 mΩ
- 标准差: 3.5 mΩ
- CPK: 1.2
- 异常 Die 主要集中在 Wafer 边缘

请设计一个 3 因子 2 水平的 DOE 实验，以优化：
1. 针尖压力 (Overdrive): 50-80 μm
2. 清洗周期 (Clean Count): 50-100 次
3. 测试时间 (Soak Time): 10-30 ms

目标：降低接触电阻标准差，提升 CPK 至 1.5 以上。
```

**输出**：
```markdown
# DOE 实验设计方案

## 实验目标
- 降低接触电阻标准差
- 提升 CPK 至 1.5 以上

## 因子与水平

| 因子 | 低水平 (-1) | 高水平 (+1) |
| --- | --- | --- |
| Overdrive (μm) | 50 | 80 |
| Clean Count | 50 | 100 |
| Soak Time (ms) | 10 | 30 |

## 实验矩阵 (2^3 全因子设计)

| 实验号 | Overdrive | Clean Count | Soak Time |
| --- | --- | --- | --- |
| 1 | 50 | 50 | 10 |
| 2 | 80 | 50 | 10 |
| 3 | 50 | 100 | 10 |
| 4 | 80 | 100 | 10 |
| 5 | 50 | 50 | 30 |
| 6 | 80 | 50 | 30 |
| 7 | 50 | 100 | 30 |
| 8 | 80 | 100 | 30 |

## 建议
- 每组实验至少测试 100 个 Die
- 重复 3 次以提高统计显著性
- 记录所有环境参数（温度、湿度）
```

---

## 八、权限与安全配置

### 8.1 权限分级策略

| 权限级别 | 适用场景 | 配置方式 |
| --- | --- | --- |
| **完全限制** | 产线相关系统 | 默认模式，每次操作都需确认 |
| **部分放开** | 团队共享代码库 | 允许读取和执行，禁止删除 |
| **完全放开** | 个人开发环境 | Auto-Approve 模式 |

### 8.2 Auto-Approve 配置

**配置方式**：
```
/install-skill auto-approve
```

**使用场景**：
- 个人本地项目开发
- 独立的 Git 分支开发
- 沙盒测试环境

**安全红线**：
- ⚠️ 严禁在产线相关系统中开启
- ⚠️ 严禁在共享代码库中开启
- ⚠️ 涉及数据库删除操作时，必须手动确认

### 8.3 权限回收机制

**建议做法**：
1. 完成任务后及时关闭 Auto-Approve
2. 定期审查权限配置
3. 建立权限使用日志

---

## 九、高级业务场景

### 9.1 技术调研与头脑风暴

**场景**：需要解决一个技术难题，但没有思路。

**指令**：
```
请作为半导体测试专家，针对以下问题进行技术调研和头脑风暴：

问题：探针台针尖磨损导致的虚焊/测量不准问题（False Contact Failure）

要求：
1. 检索并整理当前工业界解决 Micro-bump 探针损伤的主流方案
2. 列出 5 个头脑风暴方向
3. 对比各自的利弊与引入成本
4. 给出推荐的实施路径

请使用 web search 获取最新的行业资料。
```

**预期输出**：
- 技术方案对比表
- 各方案的优缺点分析
- 成本效益分析
- 推荐的实施路径

### 9.2 复杂项目开发

**场景**：需要开发一个复杂的自动化系统。

**指令**：
```
进入 Plan 模式，帮我规划一个"测试数据自动化分析平台"：

需求：
1. 支持多种数据源（CSV, Excel, SQLite）
2. 自动生成可视化报告（CPK 趋势图、Yield 分布图）
3. 支持历史数据对比
4. 异常数据自动报警
5. 输出 HTML 和 PDF 两种格式的报告

请先制定详细计划，然后进入 Build 模式执行。
```

### 9.3 知识沉淀与分享

**场景**：需要整理技术文档或制作培训材料。

**指令**：
```
请帮我整理一份"探针台常见报错排查手册"：

内容要求：
1. 列出最常见的 10 种报错
2. 每种报错的原因分析
3. 排查步骤
4. 解决方案
5. 预防措施

格式要求：
- 使用 Markdown 格式
- 包含代码示例
- 包含流程图

请基于项目中的历史 Log 文件进行整理。
```

---

## 十、团队落地路线图

### 10.1 实施阶段

```
Phase 1: 基础建设 (第 1-2 周)
├── 全员申请 CIMICode 权限
├── 安装桌面版和终端版
├── 统一配置部门级 AGENTS.md
└── 安装基础 Skill（Grill-Me, Web Search）

Phase 2: 技能培训 (第 3-4 周)
├── 组织 CIMICode 使用培训
├── 分享最佳实践和案例
├── 建立问题反馈机制
└── 制定使用规范

Phase 3: 深度应用 (第 5-8 周)
├── 推广 Plan-Build 工作流
├── 建立自动化脚本库
├── 沉淀技术文档和 SOP
└── 评估成本效益

Phase 4: 持续优化 (长期)
├── 定期复盘使用效果
├── 优化 AGENTS.md 配置
├── 扩展 Skill 应用场景
└── 分享最佳实践
```

### 10.2 成本控制措施

| 措施 | 预期效果 | 实施难度 |
| --- | --- | --- |
| 默认使用 Flash 模型 | 成本降低 60%+ | 低 |
| 配置 AGENTS.md | 减少重复输入，成本降低 20% | 低 |
| Context 预处理 | 减少输入 Token，成本降低 30% | 中 |
| 会话管理优化 | 减少无效对话，成本降低 15% | 低 |
| Auto-Approve 配置 | 提升效率，间接降低成本 | 低 |

### 10.3 效果评估指标

| 指度 | 基准值 | 目标值 | 测量方式 |
| --- | --- | --- | --- |
| 单次对话成本 | - | 降低 50% | Token 消耗统计 |
| 任务完成时间 | - | 缩短 40% | 项目周期对比 |
| 代码质量 | - | Bug 率降低 30% | 代码审查统计 |
| 文档质量 | - | 完整性提升 50% | 文档评审 |

---

## 十一、常见问题与解决方案

### Q1：Token 消耗太快怎么办？

**解决方案**：
1. 日常任务使用 DeepSeek v4 Flash
2. 复杂任务才切换至 Pro 模型
3. 使用 AGENTS.md 避免重复输入背景
4. 预处理大数据文件，只提供关键信息

### Q2：大模型回答不准确怎么办？

**解决方案**：
1. 检查 AGENTS.md 是否配置了正确的背景信息
2. 使用 Grill-Me Skill 帮助厘清需求
3. 提供更多上下文信息（如具体文件路径、错误代码等）
4. 使用 Web Search 获取最新资料

### Q3：如何评估大模型的使用效果？

**解决方案**：
1. 记录每次对话的 Token 消耗
2. 统计任务完成时间
3. 评估输出质量（代码质量、文档完整性）
4. 定期复盘并优化使用策略

### Q4：团队成员水平不一致怎么办？

**解决方案**：
1. 新人使用 Grill-Me Skill 帮助厘清需求
2. 建立团队共享的 Prompt 模板库
3. 定期组织经验分享会
4. 建立代码审查机制
